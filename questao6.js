/*
Questão 6: Escreva um código JavaScript utilizando o operador ternário para 
determinar se um número é múltiplo de 5. A variável numero contém o valor a 
ser verificado. Atribua o resultado à variável resultado.
*/

let numero = 9;
let condissao = (numero % 5 == 0) ? "Multíplo de 5" : "Não se enquadra"
console.log(condissao)